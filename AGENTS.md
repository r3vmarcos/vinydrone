# AGENTS.md

## Objetivo

Landing page One-Page Viny Drone com React, TypeScript, Vite, Tailwind CSS e Framer Motion.

## Padrões

- Componentes por seção em `src/components/sections`.
- Componentes reutilizáveis em `src/components/ui`.
- Dados editáveis em `src/data/siteData.ts`.
- Utilitários isolados em `src/utils`.
- Tailwind para layout, espaçamento, responsividade e estilo visual.

## Desenvolvimento

- Rodar `npm install`.
- Rodar `npm run dev`.
- Antes de publicar, rodar `npm run build`.
